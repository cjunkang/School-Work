javac *.java
open 2 cmd terminals

java Alice on 1st terminal (server)

java Bob on 2nd terminal (client)

It will display p,g,h,private key, public key and calculate common key on Alice
On Bob terminal, it will display private key,public key and common key.